package CoreJava.LangPackage;

public class WrapperClasses {

    public static void main(String[] args) {
        int primitive = 10;
        Integer nonPrimitive = 20;

        //valueOf is used to convert primitive to wrapper
        //We use parseInt method when we have input as String and want to convert to the primitive type //REMEMBER THE NUMBER FORMAT EXCEPTION
        //We use intValue to covert the wrapper type into the primitive kind :-)
    }
}
